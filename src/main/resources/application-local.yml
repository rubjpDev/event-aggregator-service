spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/event_aggregator
    username: admin
    password: toor
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  cache:
    type: redis

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    root: INFO
    com.rubdev: DEBUG